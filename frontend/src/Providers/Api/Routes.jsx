const urlBase = "http://localhost:8080/api";

export const routes = {
 products: `${urlBase}/products`,
 orders: `${urlBase}/orders`,
 customer: `${urlBase}/customer`,
 auth: `${urlBase}/auth`,
 register: `${urlBase}/auth/register`,
 login:` ${urlBase}/auth/login`
};
